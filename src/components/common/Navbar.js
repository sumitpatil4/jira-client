import React from "react";
import "./Sidebar.css";

const Navbar = props => {
  return (
    <header>
      {/* <button onClick={props.onClick}>Click Me!</button> */}
      Navbar
    </header>
  );
};
export default Navbar;
